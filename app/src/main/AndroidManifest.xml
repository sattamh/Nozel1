<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="Sa.Nozel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_nozel"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_nozel"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".account"></activity>
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".forgot_password" />
        <activity android:name=".Edit_profile" />
        <activity android:name=".register" />
        <activity android:name=".search_result" />
        <activity android:name=".login" />
        <activity android:name=".tool_bar_lt" />
        <activity android:name=".bottom_bar_lt" />
        <activity android:name=".main_activity"></activity>
    </application>

</manifest>